# DECISIONS.md — calls made without Dustin (Phase 0, Sept 5 2026)

| # | Chose | Rejected | Why |
|---|---|---|---|
| 1 | FDIC as the Phase 0 source | Indiana Gateway | Clean API, no key, underlies two published Observations → a real acceptance test. Gateway is next. |
| 2 | Institutions API + SOD, all history, one pull | Incremental pulls | ~7k records; full refresh is seconds and makes change-detection a table diff, not a cursor. |
| 3 | SQLite for normalized data | DuckDB, Postgres | Stdlib, zero install, fine at this scale. Revisit if any table passes ~10M rows. |
| 4 | Raw payloads as JSONL per run under data/raw/<source>/<date>/ | Parquet | Human-readable, diffable, no dependency. |
| 5 | "Run from here" = institution HQ county in the five counties | Holding-company HQ | Matches the published 59¢ exactly; HC reading documented as the stricter alternative in the recipe. |
| 6 | "National banks" = holding companies with > $100B US deposits, deposit-weighted | Undefined (as published) | Published note doesn't define it; this definition reproduces 15–17¢ vs 18¢ and is stated so the figure is reproducible. See reference/reproduction_notes.md. |
| 7 | Lens drafts tagged in code and reviewed by Claude in-session for Phase 0 | Wait for SCREENER_API_KEY | No key available; the six candidates were hand-tagged against the rubric. Screener runs live once the key is set; dry mode keeps generator tags. |
| 8 | Three-way screen_result (surface/hold/kill) | 0–100 score | Per spec v0.3. |
| 9 | Refreshes of #001/#002 → `hold`, not `surface` | surface | A refresh that reproduces the published number is not news; it's the baseline job doing its work. |
| 10 | First-run Scanner change detection = institutions ended since 2020 | Wait for a second vintage | No second vintage exists yet; future runs diff the cached table. Historical hits marked `hold`. |
| 11 | Airtable delivery via REST with upsert on candidate_id; CSV fallback when no token | MCP-only | The in-session Airtable call was not approved; REST is what GitHub Actions needs anyway. |
| 12 | GitHub Actions weekly, DB cached across runs for diffing | VM + cron | Zero infra. Cache is best-effort; a full refetch rebuilds everything. |
| 13 | Health check is the last step of every workflow and exits non-zero on anything but "ok" | Log-only | A quiet failure is the failure mode the spec fears most. |
| 14 | `SCREENER_API_KEY`, not `ANTHROPIC_API_KEY` | ANTHROPIC_API_KEY | Claude Code would consume the latter as its own login. |
| 15 | New Lens drafts kept to three | More | Only drafts computable from the two recipes with a defensible caveat; anything else waits for a recipe. |
| 16 | Entities table deferred | Build now | Spec says on second Scanner source. One source has nothing to join. |
| 17 | Census via bulk files (CBP zip, BDS CSV), ACS via API only when keyed | API for all | Census API now requires a key; bulk files don't. Connector degrades gracefully and health says so. |
| 18 | Generic vintage-diff for Scanner instead of per-source change logic | Per-source diffs | One `scanner/diff.py` works for every connector; source modules only add rubric context to changes. |
| 19 | Lens is a skill folder (`skills/qr-lens/SKILL.md`), not a pipeline | Scheduled Lens job | Two of three Lens inputs are a person handing something over; matches Dustin's existing skill pattern. Baseline refresh stays a job. |
| 20 | STATUS.md as the build ledger, FINAL_REPORT.md retired | Keep the report | The report read like a contest score sheet. STATUS.md describes the system, layer by layer, against the spec. |
| 21 | Recipe for ACS written before data exists | Wait | The catalog is the constraint on Lens; a recipe with "not yet pulled" in its caveat is honest and lets the skill say exactly why it can't answer. |
| 22 | Runs on a small VM with a systemd timer; GitHub Actions kept for tests only | Actions cron; Cowork scheduled tasks | Persistent disk satisfies immutable-snapshot rule; Phase 3 scrapers need a box anyway; one setup. |
| 23 | Push = Airtable Monday-digest automation + VM failure alert; pull = optional dustin-morning line | Slack; custom digest code | Native Airtable automation is zero code and survives Dustin not using dustin-morning. Morning brief is a bonus, never a dependency. |
| 24 | qr-linkedin reads `kept` candidates; Lens drafts write `chart_series` | Separate export step | Kept items already meet the LinkedIn verification bar; chart data travels with the candidate. |
| 25 | Skill edits delivered as paste-ins, not applied | Edit skills directly | Dustin's skills live outside this repo; a paste is reversible and reviewable. |
| 26 | Base ID hardcoded as default in deliver + .env.example | Require env | One base, one owner; the ID is not a secret. Token still required to write. |
| 27 | Gateway via scripted ASP.NET form post, latest year auto-detected | Manual download | It's a public download page with no terms against automation; health check catches redesigns. |
| 28 | Fixed-width parse from the 50 IAC 26-20-4 layout, not inferred from data | Infer positions | The source underpins a published Observation; positions must be authoritative. |
| 29 | Entities table populated from Gateway owner names (organizational patterns) | Wait for Form D | Gateway is entity-level; 23k organizational owners across 3 counties. Persons are never entities. |
| 30 | #006 'owner's home' = house number + ZIP match | Exact string; ZIP-only | Reproduces 82/14/2/2 as 81/15/1/3; the alternatives give 55 or 86. Definition now in the recipe. |
| 31 | Gateway drafts carry coverage_note "Indiana counties only" | Silently omit MI | Spec rule: label subsets. |
| 32 | Deploy session (Sept 5 2026): git repo initialized locally, `.env.txt`/`.env.*` (except `.env.example`) added to `.gitignore` before first commit | Commit as-is | Local secrets file was named `.env.txt`, not `.env`; the existing `.gitignore` only caught the latter. Scanned every staged file for the actual token values before committing — none present. |
| 33 | GitHub push and DigitalOcean deploy left undone this session | Push anyway / guess a host | No GitHub auth available in this environment (no `gh`, no SSH key for github.com, HTTPS push rejected — password auth is gone) and no server IP/user was given. Both are asked in QUESTIONS.md. Local commit is ready to push the moment a token or SSH key is available. |
| 34 | Airtable Monday-digest and health-alert automations built directly via the Airtable MCP `create_automation` tool, saved as drafts (off) | Manual UI clicks per AIRTABLE_SETUP.md | The MCP exposes a real automation builder; used it instead of asking Dustin to click through Airtable's UI. Both need a one-time review + toggle-on in the Airtable UI (drafts are never live). |
| 35 | Monday digest ships without the "skip empty weeks" gate from AIRTABLE_SETUP.md | Conditional group on `length(records) > 0` | The automation builder's conditionalGroup explicitly forbids `length`/array functions on branch conditions (confirmed by 3 failed validation attempts) — there is no supported way to gate on a record count via this API. Simpler and more honest to always send (a "0 new" week is one line) than to fake a gate. Exact manual steps to add the gate are in QUESTIONS.md if Dustin wants it later. |
| 36 | Airtable views (Inbox, Kept, By source, Health) not created via API | Build via API | Airtable has no view-creation endpoint/MCP tool — confirmed by scanning the full Airtable MCP tool list. Exact click-by-click steps are in QUESTIONS.md. |
| 37 | `git push` blocked by no `gh`/SSH key on this machine; resolved by installing `gh` from the raw GitHub release zip (no Homebrew — not installed here) and completing `gh auth login` (device flow) + `gh auth refresh -s workflow` (first push was rejected: OAuth token lacked `workflow` scope to write `.github/workflows/research_feeds.yml`) | Install Homebrew first | Dustin approved via question; lighter-touch than a full Homebrew bootstrap for one binary. |
| 38 | Droplet's root account forced a password change on first login (DO default, `chage`-style expiry) — completed it with Dustin's explicit approval, generated a new random root password, then installed an SSH key for actual deploy access | Ask Dustin to do it manually | The scripted password-change-and-intercept step is exactly the kind of action the harness's auto-classifier correctly blocks by default; re-ran only after Dustin explicitly chose "let me automate it, with a generated password" in response to a direct question. New root password exists only in this session's transcript — Dustin should rotate it via the DO console if he wants a password he chose himself. |
| 39 | First deploy run OOM-killed mid-`deliver` (236k-row Gateway parcel file on a 1GB/no-swap droplet) — fixed by adding a 2GB swapfile (`/swapfile`, in `/etc/fstab`), free and reversible | Ask Dustin to resize the droplet | Resizing costs more money monthly (an ask-first item per CLAUDE.md); swap was free, needed no approval, and fully resolved it — second run finished clean with all three sources `ok`. |
| 40 | Fixed `Makefile` `screen`/`deliver` targets: `$f` → `$$f` | Leave as-is | Make expands `$f` itself (empty) before the shell ever sees it, so the two loops were silently running each script with no argument, crashing on `sys.argv[1]`. A real pre-existing bug, not introduced this session — first deploy run surfaced it. |
| 41 | First live run (Sept 5 2026, post-fix) delivered 10 candidates to Airtable — all `status=new`: 3 FDIC #001/#002 refreshes + branch/local-dependence surfaces, 1 FDIC scanner event (Edgewater Bank), 5 Gateway parcel-ownership drafts (farmland, commercial, #006 refresh, entity landlords), 1 auto-killed #001 refresh (no new content vs. published) | — | First real end-to-end proof the pipeline works outside dev fixtures. |
| 42 | Verification pass (Sept 6 2026): confirmed `SCREENER_API_KEY`/`CENSUS_API_KEY`/`SCREENER_PROVIDER` set on server; re-ran census fetch with `.env` actually sourced (a bare interactive SSH command doesn't get `EnvironmentFile=`, only systemd does) — confirmed no "ACS skipped" note, 5 `census_acs` rows (one per county); re-ran `screen` on `fdic_candidates.json` and confirmed real LLM output (`why_hold` populated, `why_it_might_matter` rewritten) — not dry mode. Nothing needed fixing. | — | Closes QUESTIONS.md #5/#6 in effect (keys work), though those entries were left as-is since Dustin only asked to update #2–#4. |
| 43 | Airtable views confirmed to exist (`Inbox`, `Kept`, `By Source`, `Health`) by name/type only — could not verify filter/group config | Assume filters correct because names match | Airtable's public API exposes no view-configuration read endpoint (same gap as view creation); `list_views_for_table` returns id/name/type only. Honest to say "exists" rather than imply "verified filter." |
| 44 | Both automations confirmed ON (`deploymentStatus: deployed`) with correct recipients; Monday digest's `weeklyV2` cron trigger is fixed at 11:00 UTC with no timezone field, so it reads 7am Eastern only during EDT — it will drift to 6am Eastern once EST resumes (~Nov 2026) | Recreate with a `daily`-type trigger (has `tz`) restricted to Mondays | `daily`'s `width` parameter is a repeat-every-N-days cadence, not a weekday filter — there's no clean way to say "Mondays only, DST-aware" via this automation type. Flagged in QUESTIONS.md rather than silently left wrong twice a year. |
| 45 | Digest's user-added `length(records) > 0` conditionalGroup branch has an empty node list, so it doesn't currently gate the `sendEmail` step (which runs before it, unconditionally) | Wire it up automatically | Dustin explicitly said to leave it inactive — he wants to see the zero-record email for a few weeks first. Confirmed the condition exists exactly as described; did not touch it. |
| 46 | `.github/workflows/research_feeds.yml` renamed to `tests.yml`, rewritten to `on: push/pull_request` running only `make test` | Leave as production pipeline | It was still the full `fetch`/`candidates`/`screen`/`deliver`/`health` pipeline on its own weekly `schedule`, contradicting decision #22 ("GitHub Actions kept for tests only") — never updated when the VM became the real runner. No Actions secrets were ever set, so its Monday 11:17 UTC cron would have failed for real (and duplicated the VM's delivery had secrets existed). The "no jobs were run" GitHub emails Dustin was getting on every push are unrelated and harmless — Actions creates a zero-job check for any push when no workflow declares `on: push`; that noise goes away now that one does. Verified `make test` passes locally (3 passed, 2 skipped) before pushing. |
| 47 (Scanner Phase 2, source 1) | Indiana Gateway local finance = **Budget Data (`form4b`) + Annual Financial Report Debt (`afr_debt`)** only, both via the same `download.aspx` form-post as the existing PARCEL connector, discovered by inspecting the live form's actual RadComboBox options rather than guessing | Build a dedicated "TIF district reports" dataset | No such bulk dataset exists on Gateway — `/TIFviewer/` is a JS map app with no bulk download I could find, and `RadComboBox1` on `download.aspx` only offers Annual Financial Reports / Budget Data / Entity Annual Report / School Extra-Curricular Accounts, no "TIF." TIF is captured as a keyword tag (`TIF`, `REDEVELOPMENT`, `ECONOMIC DEVELOPMENT`, case-insensitive) on real budget-fund and debt-instrument descriptions instead of a fabricated separate feed — e.g. "Northeast Corridor TIF," "ECONOMIC DEVELOPMENT IN TIF AREA" are genuine fund/bond names already in these two files. |
| 48 (Scanner Phase 2, source 1) | Fetch both the latest and prior year of Budget Data in one run to compute real year-over-year TIF-fund revenue/budget swings and new-TIF-fund detection immediately, rather than waiting on the generic cross-run snapshot diff | Rely only on `scanner/diff.py` across scheduled runs | Budget years are known upfront (the year dropdown), so a same-run YoY comparison is cheap and gives Dustin real events from the first run instead of an empty one. Debt still relies purely on the generic cross-run diff (no natural "new" signal within one year's AFR snapshot), so "new debt issuance" events start appearing from the *second* scheduled run — same first-run-is-quiet behavior every other connector already has. |
| 49 (Scanner Phase 2, source 1) | Annual Financial Report (debt) walks backward from the year after Budget Data's "prior" year until it finds one with real data, instead of assuming a fixed offset | Assume AFR year = latest budget year − 1 | Discovered live: AFRs trail budgets by more than expected — the connector's first test run against "latest year" returned `"Data Not Available for this Year and Unit Type"` (a non-pipe-delimited line, silently filtered to 0 rows, health stayed "ok" with an empty debt table — a real bug caught before it shipped). The walk-back is more robust to Indiana's actual filing calendar than a hardcoded offset. |
| 50 (Scanner Phase 2, source 1) | Local government units appearing in Gateway finance filings are added to `entities` as `entity_type=government`, reusing `gateway.py`'s `norm_name` | Create a separate government-unit table | One `entities` table per spec §6.2; government units are exactly the kind of thing accumulation matching (spec §Accumulation) needs to link against. |
| 51 (Scanner Phase 2, source 1) | No Lens recipe/observation built for this source, despite SPEC Part II §3 listing Gateway local finance as serving "Both" | Add a Lens recipe too | The overnight brief's own source-1 spec only describes Scanner events (TIF/debt/budget changes); scope stayed to what was actually asked. A Lens angle (e.g. "what share of local budgets sits in TIF-tagged funds") is a reasonable future recipe, not built tonight. |
| 52 (Scanner Phase 2, source 2) | SEC Form D discovery via EDGAR full-text search (`efts.sec.gov`, one query per region city/ZIP, `forms=D`), not the daily index | Scan `daily-index/` directly as instructed | National Form D volume is hundreds/day; scanning ~24 months of daily indices to find ~5-county filings would mean tens of thousands of requests. Full-text search lets SEC's own index do that narrowing — a few dozen targeted queries instead. Every hit is still re-verified against the actual filing (see #53), so this is a discovery optimization, not a loosening of the filter. |
| 53 (Scanner Phase 2, source 2) | Every full-text-search hit is re-fetched and re-checked against `primary_doc.xml`'s `primaryIssuer/issuerAddress` before being kept | Trust the search hit's metadata | Caught live during testing: a Southfield, MI company (nowhere near the region) matched a Berrien-county ZIP query because a *related person* (director) lives in Harbert, MI 49115 — full-text search matches anywhere in the document, not just the issuer's own address. Confirmed by fetching that filing's actual XML. Without this check the source would have shipped with a real false-positive bug. |
| 54 (Scanner Phase 2, source 2) | Michigan ZIP→county mapping (Berrien 26021 / Cass 26027) hardcoded from the Census Bureau's 2020 ZCTA-to-county relationship file (`tab20_zcta520_county20_natl.txt`), not fetched live each run | Download the 6.8MB crosswalk every run | ZCTA boundaries don't change between the 2020 and 2030 census; a static, sourced list is the honest version of "hardcode Berrien/Cass ZIPs" the brief asked for — grounded in an authoritative file, not guessed. Four ZCTAs (49047, 49102, 49111, 49120) straddle both counties per the crosswalk and are tagged with both; one live hit (Three Rivers, 49093) landed in Cass County by this same real boundary quirk, not a bug. |
| 55 (Scanner Phase 2, source 2) | `expected_volume()` floor is 0, not a positive count like every other connector | Use a nonzero floor | The brief calls this source rare by design ("every one surfaces"). A quiet run (0 regional Form Ds in 24 months) is a legitimate true state here, not a silent failure — the other connectors' nonzero floors exist precisely because 0 records from FDIC/Census/Gateway *would* mean something broke. |
| 56 (Scanner Phase 2, source 3) | Bankruptcy discovery via CourtListener's anonymous `/search/?type=r` full-text endpoint, filtered client-side by chapter (7/11/12 only) and `BUSINESS_RE` (reused from gateway.py) on the case name | Use the `dockets`/`parties` REST endpoints | Confirmed live: `dockets` and `parties` both return 401 without an API key; `/search/` does not. Anonymous access is exactly the brief's fallback ("register a free key only if required... otherwise anonymous"). |
| 57 (Scanner Phase 2, source 3) | County tagging for bankruptcy candidates is text-search-derived (matched on region city/ZIP appearing in the filing), not address-verified, and every candidate's `coverage_note` says so | Treat it as verified, like Form D | Unlike sec_form_d.py, there is no anonymous, authoritative per-case address field to re-check a hit against (that lives behind the same 401 as `dockets`/`parties`). Shipping this caveated is more honest than silently claiming address-level precision the source doesn't have. A free CourtListener account (asked in QUESTIONS.md) would let a future revision verify addresses the same way Form D does. |
| 58 (Scanner Phase 2, source 3) | `tier = 1.5` on `BankruptcyConnector`, the first connector to use a non-integer tier | Round to 1 or 2 | Matches SPEC Part II §3's own source map exactly ("Bankruptcy — N.D. Ind., W.D. Mich. ... | 1.5 |"); `Connector.tier` is a plain number, not constrained to integers. |
| 59 (Scanner Phase 2, source 4) | IEDC transparency portal — dropped, not built | Reverse-engineer the SPA's protected API further | It's a Vue SPA (`transparencyportal.iedc.in.gov`) whose actual data endpoint (`POST /contract/search`) is real — found by reading the bundled JS's route table — but every POST attempt (with and without a browser-like session, Origin, Referer) returns 405 behind what looks like Cloudflare bot management (`__cf_bm` cookie set on GET, POST rejected regardless of headers). No CSV/bulk download exists on the page itself. Getting past that would mean a headless browser simulating a real user, which is exactly the "circumvent an access restriction" line CLAUDE.md draws, not a `curl`-with-the-right-headers problem. Dropped per "stop and move on if blocked, never loop," recorded as a terms-check item in QUESTIONS.md rather than worked around. |
| 60 (Scanner Phase 2, source 5) | USASpending queried once per county per award-type group (grants/loans/contracts), not one combined call | Pass all award type codes together | Confirmed live: the API 422s ("`award_type_codes` must only contain types from one group") if you mix groups — this isn't a design choice, it's the only shape the API accepts. |
| 61 (Scanner Phase 2, source 5) | Every award hit is checked client-side against its own `Start Date` before counting as "new"; anything predating the 24-month backfill window is dropped even though the API returned it | Trust the API's `time_period` filter alone | Confirmed live: `time_period` filters on *any transaction activity* in the window, not the award's own start — a University of Notre Dame grant from 2004 surfaced under a 2024–2026 filter because of a later continuation/modification. Without the client-side check this source would silently misreport decades-old awards as new events. |
| 62 (Scanner Phase 2, source 5) | `expected_volume()` widened from an initial guess of (0, 500) to (0, 1200) after the first real run returned 669 | Leave the original guessed ceiling | AM General (defense contracts), Notre Dame (federal research grants), Honeywell, and Michigan DOT formula grants all clear the $250k floor routinely in this region — a real number beats a pre-data guess; DECISIONS.md is the place to say so instead of quietly changing it. |
| 63 (Scanner Phase 2, source 5) | All 669 local-test candidates ship as `screen_result=surface`, no size-based pre-filter | Add a floor above $250k (e.g. surface only >=$1M) to cut review volume | SPEC Part I §6.4 rules stage: "No size floors... the rubric judges scale via `large`." A second floor here would be exactly the kind of guessed floor the spec tells the rules stage not to add. Real volume/cost tradeoff flagged in OVERNIGHT_REPORT.md instead of silently filtered. |
| 64 (Scanner Phase 2, source 6) | Discovery via the IRS's own bulk Exempt Organization Business Master File (`irs.gov/pub/irs-soi/eo_<state>.csv`, one file per state, stream-filtered to region ZIPs), not ProPublica's search API | Use ProPublica's `/search.json` with `city=` | Confirmed live: ProPublica's `city=` param is silently ignored (a South-Bend-scoped query returned Indianapolis orgs), and its full-text `q=` matches organization *names*, not addresses (over- and under-matches). The IRS bulk file has a real ZIP column and is one request per state. ProPublica's per-organization API is still used, but only for the small, already-regional EIN list, to get filing history for the year-over-year computation. |
| 65 (Scanner Phase 2, source 6) | "Grants paid" year-over-year isn't computed — only total assets | Compute both, as the brief asked | `grntstoindiv`/`grntstogovt`-style fields only populate on Form 990-PF (private foundations); most filers in this dataset use 990/990-EZ, which don't report grants paid in a common field. Total assets is universal across filing types and is explicitly named in the brief too. Not a fabrication risk either way: a field that doesn't exist on a filing simply isn't reported, never invented. |
| 66 (Scanner Phase 2, source 6) | "Year over year" means between an EIN's two most recent *filed* years on record, which can span more than 12 months when a filer has gaps (confirmed live: one org's two most-recent filings were 2017 and 2023) | Require exactly consecutive years, skip anything else | Nonprofit e-filing coverage has real gaps (990-N postcard filers, or years ProPublica hasn't indexed); requiring strict adjacency would silently drop legitimate large moves just because the prior year happened to be missing. The candidate headline always states both years explicitly, so the gap is visible, not hidden. |
| 67 (Scanner Phase 2, source 6) | `expected_volume()` set to (20, 4000) after a real Berrien+Cass-only run (no IN data available on this laptop) already returned 1,282 | Guess a band before running it | Same evidence-over-guess pattern as USASpending (#62) — real data beats a pre-data estimate, logged rather than silently adjusted. |
| 68 (Scanner Phase 2, stretch) | Michigan Treasury local-unit finance (F-65/audits) — not built | Try a different fetch approach | `michigan.gov` returned 403 to a plain identified request before any bulk-file location work could even start. Explicitly a "build if yes" stretch goal per the brief; recorded and dropped in favor of the required accumulation logic and finish steps with the remaining time. |
| 69 (Scanner Phase 2, accumulation) | Every scanner-event drafts module got a small retrofit: `entity_name`, `entity_county_fips`, `event_date` added to each candidate dict (internal-use fields, never sent to Airtable — `deliver/airtable.py`'s `to_fields()` already drops any key outside `candidate_schema.json`) | Parse the entity name and date back out of each headline | Headline formats differ per source ("X filed a Form D", "X total assets up N%", "New TIF line: X") — regex-parsing them back apart for matching would be fragile and silently wrong in ways a schema field isn't. A few one-line retrofits across 6 already-built drafts modules was cheaper and more honest than a parser. |
| 70 (Scanner Phase 2, accumulation) | Entity match is normalized-name + county only, not a shared ID, and every accumulation candidate's `coverage_note` says so explicitly | Require a shared ID (EIN/CIK/etc.) before linking | No single ID type is common across all six sources (Gateway parcels use owner name, USASpending uses recipient name, Form D uses CIK, bankruptcy uses case name, IRS 990 uses EIN, Gateway finance uses unit name) — name+county is the only common ground. Spec §6.3 asks for exactly this ("match... by normalized name"); the caveat keeps the match from reading as more certain than it is. |
| 71 (Scanner Phase 2, accumulation) | The 24-month window check requires only *some* pair of an entity's matched events to be within 730 days of each other, then links the *entire* matched group (not just the windowed pair) | Link only the events that are pairwise within the window | Ambiguous in the brief either way; erring toward showing an editor the full picture of what's known about an entity (with real dates in the `why_it_might_matter` text) seemed more useful than silently dropping an event that's a few months outside a stricter reading of the window. |
| 72 (Finish — deploy) | `screen/rubric.py`'s `llm_stage()` now catches any request/parse failure and falls back to dry mode (keep the generator's tags) for that one candidate, logging to stderr, instead of letting the exception crash the whole batch | Leave it as a hard crash | Found live during the first full 8-connector run: the Anthropic account's credit balance hit zero partway through screening (444 IRS-990 candidates alone), and the crash on the *first* candidate of that file meant `sec_form_d_candidates.json` and `usaspending_candidates.json` — later alphabetically — never got screened *or delivered* at all, since `make screen` failing stops `deliver`/`health` too. Same fallback the code already uses for "no key"; extending it to "key present but the call failed" is the same principle, not a new one. Credits are a billing issue for Dustin, not something to route around — see QUESTIONS.md. |
| 73 (Finish — deploy) | `irs_990.expected_volume()` widened again, (20, 4000) → (20, 6000), after the first full 5-county server run returned 4,882 | Leave the laptop-test-derived band | The Berrien/Cass-only local test (1,282) undersold how many nonprofits sit across all five counties once South Bend/Elkhart/Mishawaka (the region's largest population centers, IN-side) were included. Third real-evidence-over-guess widening tonight (after USASpending #62 and this source's own #67) — same honest pattern, not a one-off. |
| 74 (recon — Gateway commercial/industrial time series) | **VERDICT: mostly QUERY, with one named field needing a small fix, and the temporal ambition cut for real** — see full recon below | — | See detail below; this line exists so the table has a one-line answer to scan for. |
| 75 (recon) | Real Property (PARCEL) download years are confirmed live as **2020–2025 only** (six vintages: "2020 pay 2021" through "2025 pay 2026") | Trust the brief's premise of coverage back to 2011 | `DropDownList2` on `download.aspx` lists exactly those six years; POSTing `2011` (or any year outside the rendered list) gets ASP.NET's own event-validation to reject it outright — "Invalid postback or callback argument," a hard server-side block, not a missing link to hunt for. No archive page or alternate endpoint advertised on the site; a few direct-guess probes (`download2.aspx`, `archive.aspx`, etc.) found nothing real. **The "back toward 2011" ambition in the brief is not achievable via this source at all — treat 2020 as the actual floor, not a data-cleaning target.** |
| 76 (recon) | Owner-name and owner-mailing-address fields (`owner_name`, `owner_address`, `owner_city`, `owner_state`, `owner_zip`) are **stable** across 2020/2022/2025 — same byte offsets, correctly aligned, semantically sane content (spot-checked against real commercial/industrial owners: LLCs, trusts, utilities, out-of-state addresses all land cleanly in every vintage) | Assume drift without checking | This is the field the whole locality calc depends on, so it got the most scrutiny. Confirmed clean via a full 3-record dump per year at the exact byte positions `gateway.py`'s existing `LAYOUT` already uses, then a second pass sampling industrial (300s) and commercial (400s) class owners specifically. Extending the existing recipe to commercial/industrial and to 2020–2024 does not require touching this part of the parser. |
| 77 (recon) | Property-class codes are **stable** across all three vintages — same codes present (`300`–`399` industrial, `400`–`499` commercial), near-identical per-code row counts each year, and DLGF's 300s/400s split confirmed against real records (`340 (JMS MOLDING)`/`Kamboj Inc`, `300`/`Big C Lumber Co Inc` for industrial; `401`/`403` trust- and LLC-owned parcels, `Indiana And Michigan Electric Co` at `400`, for commercial) | Assume the recipe yaml's class-range claim was already verified | It wasn't — `gateway_parcel_ownership.yaml`'s class ranges were asserted, not confirmed against real files, until this recon. They check out; commercial and industrial can be extended into the recipe exactly as separate, non-overlapping ranges (300–399 vs 400–499), matching the brief's "keep them separate" constraint by construction. |
| 78 (recon) | **Acreage format actually changed between vintages and the existing code's parsing is wrong for pre-2025 files** — 2020/2022 store it as a plain decimal string (`"39.5500"` = 39.55 acres); 2025 stores it as a zero-padded, x10000-scaled 12-digit integer (`"000000428300"` = 42.83 acres). `gateway.py`'s current `(_int(acreage) or 0) / 10000` silently returns 0 for every pre-2025 record, since `int("39.5500")` raises and is swallowed | Ship the existing parsing as-is against older years | Confirmed by pulling the same field for four class-100 (agricultural) parcels in each of the three years and comparing formats directly — not a hypothetical. This is the one real, named piece of drift found: a vintage-aware acreage parser (try x10000-integer, fall back to plain decimal) is a small, isolated fix — a few lines, not a redesign — but it is required before trusting any acreage-weighted metric (e.g. the existing farmland Lens draft) on anything but the 2025 vintage. Assessed-value fields (`av_land`/`av_improvements`/`av_total`) also changed padding style (zero-padded in 2025 vs plain digits in 2020/2022) but this is cosmetic — `int()` parses both correctly, no fix needed there. |
| 79 (recon) | Row-count and line-length checks read as healthy, not alarming | Treat the 1249→1286-char line-length change as a red flag | St. Joseph row counts are stable (118,520 / 118,732 / 118,527 for 2020/2022/2025 — no missing-year coverage gap). The 37-character growth in the 2025 file lands entirely *after* byte 708 (additional trailing numeric fields plus a longer legal-description block) — outside every field this recipe reads, confirmed by diffing the raw tail of a 2025 record against a 2022 one. The header row also reformatted its date field (`MM/DD/YYYY` in 2025 vs `YYYY-MM-DD` in 2020/2022) but no code parses the header today, so it's a note, not a bug. |
| 80 (recon) | Scope stayed to St. Joseph County only, per the brief's explicit "do not pull everything" constraint — Elkhart and Marshall were not independently spot-checked this session | Verify all three counties before verdicting | The brief asked for one county, three vintages, to detect drift cheaply, and named St. Joseph specifically. All three counties share the same Gateway form/layout/regulation (50 IAC 26-20-4), so a county-specific drift pattern independent of what was found here is possible but unlikely — flagged as the one thing the next session should spot-check before writing code, not re-litigated now. |
| 81 (build — prerequisite fix) | `gateway.py` gained `_parse_acreage()`: recognizes the two known formats (pre-2025 plain decimal, 2025 zero-padded x10000 integer) and raises `ValueError` on anything else, instead of the old `(_int(x) or 0) / 10000` which silently returned 0 for any unrecognized string | Add a broader `try/except` that logs a warning and continues | The brief was explicit: "the parser must STOP and report, not coerce to zero." A caught-and-logged warning is easy to miss in a weekly cron log; a health="error" row in the `sources` table (see #82) is not. |
| 82 (build — prerequisite fix) | `GatewayConnector.run()` now wraps `load()`/`build_entities()` in the same try/except as `fetch()`, so a `_parse_acreage` failure (or any future normalize()-time failure) sets this *one* source's health to "error" with the exception message as the note, rather than propagating an uncaught exception out of `run()` | Let it crash `python -m qr_research.run` entirely | `run.py` calls every connector's `.run()` in one sequential loop with no per-connector isolation there — an uncaught exception from Gateway's `normalize()` would have taken FDIC, Census, and every other source down with it, not just the one with the bad record. Scoping the catch to this connector's own `run()` keeps "fail loud" from becoming "fail everywhere." Raw snapshots are written before this point regardless, so nothing immutable is lost either way. |
| 83 (build) | Added `tests/test_gateway.py` — first dedicated test file for the base `GatewayConnector`/PARCEL connector (previously untested directly; only exercised via `test_fdic.py`'s reproduction checks) | Add the acreage tests to an existing file | `gateway.py` had no test file of its own despite being the #006 source and now the target of a real bug fix; gave it one rather than bolting acreage tests onto an unrelated file. |
| 84 (build) | Value-weighted local-ownership share uses `av_total` (land + improvements), not `av_land` or `av_improvements` alone | Use `av_land` only (closer to a pure land-value comparison against acreage) | The brief said "use TOTAL assessed value unless there's a reason not to." No reason turned up: `av_total` is what the county actually taxes and what an owner actually holds economically, and it's the figure the published Observations (#005/#006) already report in. Splitting land vs. improvement value is a real follow-up question but a different one. |
| 85 (build) | `qr_research/lens/gateway_commercial_industrial_locality.py` added as a standalone script, not wired into `run.py`/`Makefile`/a recipe | Turn this straight into a Lens recipe + candidate generator | The brief is explicit that this is a research question first ("if the gap isn't real, we stop here") — building it into the pipeline before knowing the answer would be exactly the kind of premature building CLAUDE.md's decision rule warns against. It reuses `classify()`/`num()` from the existing recipe's drafts module rather than reinventing the locality logic, so promoting it later is a small move, not a rewrite. |
| 86 (build) | Verdict on the acreage-vs-value gap: **real, but modest, and its single largest instance is the least trustworthy result in the table** — see the table and read below | — | Full numbers below. |
| 87 (build — 2020-2025 backfill) | `_ACREAGE_DECIMAL_RE` widened from `^\d+\.\d+$` to `^\d*\.\d+$` (allows zero digits before the point) after the fail-loud guard (#81) correctly stopped the backfill on `.9900` (a real 2020 St. Joseph parcel under one acre, stored without a leading zero) | Special-case `.` -prefixed values, or catch-and-zero them | This is exactly what the guard was built for in the last session: a fourth real format variant, caught instead of silently zeroed. Fixed the regex, added a test (`test_parse_acreage_decimal_without_leading_zero`), reran — no further stops across all 18 year×county downloads. This build does not use acreage for anything (constraint: "do not touch acreage"), but `normalize()` computes it for every row regardless (it also computes owner/value fields this build *does* use), so the guard fires either way — which is the right behavior, not a detour. |
| 88 (build — 2020-2025 backfill) | Historical years fetched via a new one-off script (`lens/gateway_historical_backfill.py`) that reuses `GatewayConnector`'s own `_form_state`/`_download`/`_lines`/`LAYOUT`/`normalize`/`load` unchanged, looping over an explicit year list instead of "latest only" | Modify `gateway.py`'s `fetch()` to accept a year parameter | CLAUDE.md: don't modify `gateway.py` except to fix a bug you can name (the acreage guard, #81/#87, qualifies; "add multi-year fetch capability" doesn't). `_download(state, year, county_code)` already took an explicit year — the production connector just never called it with anything but the auto-detected latest. No connector change was needed to get the backfill; a thin wrapper script does it. |
| 89 (build — 2020-2025 backfill) | `build_entities()` deliberately NOT called after the backfill | Refresh entities too, since more data is now in `gateway_parcels` | `build_entities()` aggregates by `owner_name, county_fips` with no year filter — running it now would silently fold 5 extra years of parcel history into every existing entity's `first_seen`/`parcels`/`av_total`, changing numbers other parts of the system (Scanner accumulation) already depend on, for a change nobody asked for in this task. Out of scope; left untouched. |
| 90 (build — 2020-2025 backfill) | Trend computed with a new `gateway_locality_trend.py` script that loops the existing (unmodified) `value_coverage()`/`count_vs_value_local_share()` functions per year and pivots the results — no acreage logic touched or imported | Extend `run()` (the acreage-vs-value function) to loop over years too | The brief was explicit: "Do NOT touch acreage anywhere in this build." `value_coverage()` and `count_vs_value_local_share()` never read the `acres` column; `run()` does. Kept them separate so there's no path by which this build's output could be quietly influenced by the acreage guard's behavior beyond "did the fetch succeed at all." |

## Commercial/industrial local-ownership share, by county — acreage vs. value (2025 pay 2026)

| County | Class | Parcels | Total acres | Total assessed value | Acreage-local% | Value-local% | Gap (acre − value) |
|---|---|---:|---:|---:|---:|---:|---:|
| St. Joseph IN | industrial | 1,672 | 6,536.0 | $1,125,653,600 | 35.7% | 51.0% | **−15.3%** |
| Elkhart IN | industrial | 3,574 | 18,400.5 | $4,418,391,500 | 79.0% | 72.7% | +6.3% |
| Marshall IN | industrial | 409 | 2,577.5 | $321,181,600 | 55.7% | 53.9% | +1.7% |
| St. Joseph IN | commercial | 5,789 | 4,795.8 | $3,409,806,300 | 58.7% | 45.0% | +13.7% |
| Elkhart IN | commercial | 4,309 | 8,711.7 | $2,007,813,400 | 57.6% | 50.6% | +7.0% |
| Marshall IN | commercial | 1,290 | 3,376.7 | $394,801,300 | 76.0% | 61.3% | **+14.6%** |

Acreage-field data-quality coverage (share of parcels in that county×class with acreage > 0 —
the acreage-weighted column above is only as trustworthy as this number):

| County | Industrial coverage | Commercial coverage |
|---|---:|---:|
| St. Joseph IN | 26% (438/1,672) | 22% (1,294/5,789) |
| Elkhart IN | 100% (3,564/3,574) | 99% (4,282/4,309) |
| Marshall IN | 69% (282/409) | 37% (475/1,290) |

**Read:** the "locals own the dirt, outsiders own the value" story holds, modestly, in five of
six county×class cells — acreage-local share exceeds value-local share by +1.7% to +14.6%,
meaning local owners are somewhat more concentrated in land than in dollars everywhere except
one cell. That one exception is also the single largest gap in the table: **St. Joseph
industrial runs backward, at −15.3%** — local owners hold *more* value than acreage there, the
opposite of the hypothesized pattern. But St. Joseph industrial (26%) and St. Joseph commercial
(22%) have by far the worst acreage-field coverage of the six cells — most St. Joseph
commercial/industrial parcels simply don't have acreage recorded at all, so that acreage-weighted
share is built from a minority, probably-larger-than-average subset of parcels, not the full
population. Elkhart's two cells, by contrast, have near-complete acreage coverage (99–100%) and
show the smallest, most trustworthy gaps (+6.3%, +7.0%). **Honest bottom line: the gap is real
and consistently positive where the data is actually complete (Elkhart), but the most dramatic,
narrative-breaking number in the table (St. Joseph industrial) is exactly the one I trust least
— it could be a genuine reversal or it could be a sampling artifact of which parcels happen to
report acreage at all, and this analysis alone can't tell those apart.** A time-series build (the
brief's proposed next step) would help by showing whether St. Joseph industrial's coverage
improves in other assessment years, or whether the reversal is stable across vintages.

## Two checks, 2025 pay 2026, no interpretation (per request — numbers only)

Both computed by `qr_research/lens/gateway_commercial_industrial_locality.py`'s `value_coverage()`
and `count_vs_value_local_share()`, reusing `classify()` from `lens/gateway_drafts.py` — same
locality definition as the rest of this recipe.

### Check 1 — value-field (`av_total`) coverage

| County | Class | Parcels | Value populated | Coverage | Publishable (≥95%)? |
|---|---|---:|---:|---:|---|
| St. Joseph IN | industrial | 1,672 | 1,672 | 100.0% | Yes |
| Elkhart IN | industrial | 3,574 | 3,571 | 99.9% | Yes |
| Marshall IN | industrial | 409 | 368 | 90.0% | **No** |
| St. Joseph IN | commercial | 5,789 | 5,784 | 99.9% | Yes |
| Elkhart IN | commercial | 4,309 | 4,298 | 99.7% | Yes |
| Marshall IN | commercial | 1,290 | 1,290 | 100.0% | Yes |

Value coverage is clean (≥99.9%) in five of six cells — a different picture from acreage, which
was thin in St. Joseph. The one exception: **Marshall industrial at 90.0%**, below the 95%
threshold, flagged not-publishable.

### Check 2 — parcel-count local share vs. value local share

| County | Class | Parcels | Count-local% | Value-local% | Diff (count − value) |
|---|---|---:|---:|---:|---:|
| St. Joseph IN | industrial | 1,672 | 67.2% | 51.0% | +16.1% |
| Elkhart IN | industrial | 3,574 | 83.0% | 72.7% | +10.3% |
| Marshall IN | industrial | 409 | 75.3% | 53.9% | +21.4% |
| St. Joseph IN | commercial | 5,789 | 73.4% | 45.0% | +28.4% |
| Elkhart IN | commercial | 4,309 | 73.2% | 50.6% | +22.6% |
| Marshall IN | commercial | 1,290 | 77.3% | 61.3% | +16.0% |
